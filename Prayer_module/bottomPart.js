export function init(container) {
    if (!container) return;

    // Example content for the top part
    container.innerHTML = '<h1>Bottom Section</h1>';
    container.style.backgroundColor = 'lightblue';
    container.style.padding = '10px';
}
